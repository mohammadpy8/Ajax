import React, { useState } from "react";
import Child from "./Child";

const ErrorBoundary = () => {
    
    const [error, setError] = useState([]);

    return (
        <div>

        </div>
    )
}